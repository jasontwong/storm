namespace :stats do 
  namespace :store do
    # {{{ desc "Store: Generate stats for a store"
    desc "Store: Generate stats for a store"
    task :generate, [:key] => %w{surveys} do |t, args|
    end
    
    # }}}
  end
end