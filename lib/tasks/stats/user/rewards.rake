namespace :stats do
  namespace :user do
    namespace :rewards do
      # {{{ desc "User: Rewards available"
      desc "User: Rewards available"
      task :available do
      end

      # }}}
      # {{{ desc "User: Rewards redeemed"
      desc "User: Rewards redeemed"
      task :redeemed do
      end

      # }}}
    end
  end
end
