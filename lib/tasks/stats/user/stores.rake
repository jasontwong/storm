namespace :stats do
  namespace :user do
    namespace :stores do
      # {{{ desc "User: Store visits"
      desc "User: Store visits"
      task :visits do
      end

      # }}}
      # {{{ desc "User: Store unique visits"
      desc "User: Store unique visits"
      task :unique_visits do
      end

      # }}}
    end
  end
end
